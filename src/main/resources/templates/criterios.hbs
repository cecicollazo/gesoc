{{#partial "contenido"}}
    <div class="container">
        <div class="list-group">
            {{#each criterios}}
                <a href="/criterios-categorizacion/{{nombre}}"
                   class="list-group-item list-group-item-action">{{nombre}}</a>
            {{/each}}
        </div>
    </div>
{{/partial}}

{{>layout.hbs}}
